function numberType(number) {
    var number = 0;
    // Check for Postive Number
    if (number>0) {
        console.log("The Number is Postive :" +number);

    // Check for Negative Number 
    } else if (number<0) {
      console.log("The number is Negative :" +number);

    // Check for Netural number 
    } else (number==0)
    {
        console.log("The number is netural :" +number);
    }
        
} 
numberType();
